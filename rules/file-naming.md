# File Naming

Use kebab-case for all file names: `my-file-name.md`, not `MyFileName.md` or `my_file_name.md`.

This applies to all file types unless an external convention requires otherwise (e.g., CLAUDE.md, README.md).
